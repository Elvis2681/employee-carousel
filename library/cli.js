const inquirer = require('inquirer');
require('console.table');

class CLI {
  constructor(db) {
    this.db = db;
    this.logged_in_user = undefined;
  }

  start() {
   
  }
}

module.exports = CLI;
